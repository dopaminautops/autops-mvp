# autops-mvp minimal backend

This is a minimal FastAPI scaffold created for development/testing inside the devcontainer.

Quickstart

```bash
cd pwa-app/backend
./run.sh
```

The server will be available at `http://0.0.0.0:8000` (endpoints: `/` and `/health`).
